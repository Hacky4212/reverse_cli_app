# Language Split

本项目按职责分三层：

- C：权限相关、底层相关、内核相关、文件格式探测、需要贴近系统边界的代码。
- C++：性能敏感，但不直接碰权限边界的模块。
- Python：界面、命令行、用户交互、编排、报告输出。

约束：

- 低层模块不自动做提权。
- UI 层不直接承担底层解析。
- 性能模块不接管权限逻辑。

当前对应关系：

- `native/native_probe`: C
- `reverse_framework/*`: Python

后续新增模块时，先按这条分层放置，再实现功能。
